{
  "name": "cobranzaventas",
  "version": "1.0.0",
  "private": true,
  "description": "CobranzaVentas - control de ventas, abonos y saldos pendientes",
  "scripts": {
    "build": "npm run sync",
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios",
    "web": "npx serve ."
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "serve": "^14.2.4"
  }
}
